const char WIFI_SSID[] = "";
const char WIFI_PASS[] = "";

// See the Settings tab in AWS Core IoT to get the broker endpoint
const char MQTT_BROKER[] = "";

// Generate a private key and CSR with CreateCSR.ino
// Create a certificate using the CSR in AWS Core IoT 
const char CERTIFICATE[] = R"(
)";
